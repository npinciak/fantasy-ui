{
  "name": "sports-ui-sdk",
  "version": "1.0.1",
  "description": "An npm package to hold shared types, methods, and constants for the UI",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "npm run clean && tsc && npm pack",
    "build:patch": "npm run bump:patch && npm run build",
    "build:minor": "npm run bump:minor && npm run build",
    "build:major": "npm run bump:major && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf ./dist",
    "bump:patch": "npm version patch",
    "bump:minor": "npm version minor",
    "bump:major": "npm version major"
  },
  "author": "Nathan Pinciak <nathan.pinciak@outlook.com>",
  "license": "ISC",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.48.2",
    "@typescript-eslint/parser": "^5.48.2",
    "eslint": "^8.23.1",
    "prettier": "^2.7.1",
    "typescript": "^4.8.3"
  }
}
