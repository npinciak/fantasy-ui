{
  "$schema": "http://json-schema.org/schema",
  "id": "FeatureSchematic",
  "title": "Feature Schema",
  "type": "object",
  "description": "Creates an empty feature module",
  "properties": {
    "featureName": {
      "type": "string",
      "description": "New name for the feature",
      "x-prompt": "What do you want to name your feature? (e.g. entering 'NewCountry' here will make a 'NewCountryfeature')"
    },
    "folderName": {
      "type": "string",
      "description": "Name of the top-level folder this feature will be saved under",
      "x-prompt": "What top-level folder should this feature be saved under?"
    }
  },
  "required": ["featureName", "folderName"]
}
