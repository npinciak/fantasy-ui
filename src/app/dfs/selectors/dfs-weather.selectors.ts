import { GenericSelector } from '@app/@shared/generic-state/generic.selector';
import { DfsWeatherState } from '../state/dfs-weather.state';

export class DfsWeatherSelectors extends GenericSelector(DfsWeatherState) {}
